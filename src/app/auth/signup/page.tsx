import SignUpForm from '@/components/auth/SignUpForm';

export const metadata = {
  title: 'Sign Up | PulseFlow',
  description: 'Create a new PulseFlow account',
};

export default function SignUpPage() {
  return <SignUpForm />;
}
